#Run the following in your venv to install necessary dependencies if you want to run it locally
pip install streamlit
pip install pandas 
pip install scikit-learn 
pip install pickle 
pip install plotly

#How to run the program
Under terminal with your venv activated, type in streamlit run deployment.py

press ctrl+c to shutdown the program in the terminal
